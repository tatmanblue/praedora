using Xunit;

namespace Praedora.IntegrationTests;

// Placeholder — proves the project compiles and dotnet test runs end-to-end. Real
// WebApplicationFactory-based integration tests arrive alongside the features they cover.
public class PlaceholderTests
{
    [Fact]
    public void IntegrationTestProject_IsWiredUp()
    {
        Assert.True(true);
    }
}
